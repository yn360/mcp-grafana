name: Build and Push Docker Image

on:
  workflow_dispatch:
  push:
    branches: [master]
    paths:
      - "gerrit_mcp_server/**"
      - "tests/**"
      - "Dockerfile"
      - "pyproject.toml"
      - "VERSION"
      - "requirements.txt"

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Read version
        id: ver
        run: echo "version=$(cat VERSION)" >> "$GITHUB_OUTPUT"

      - name: Log in to registry
        uses: docker/login-action@v3
        with:
          registry: docker.yektanet.tech
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            docker.yektanet.tech/mcp-servers/grafana:${{ steps.ver.outputs.version }}
            docker.yektanet.tech/mcp-servers/grafana:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
